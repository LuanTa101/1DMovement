class Avatar extends Phaser.Scene {
    constructor() {
        super("first");
    }
 
     preload() {} // Load assets, called once
 
     create() {}  // Create game objects, called once
 
     update() {}  // Called N times/second
 }
 